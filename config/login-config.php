<?php

define('LOGIN_ATTEMPTS', 7);
define('LOGIN_BAN', 60);

// REGISTRATION

define('REGISTRATION_VERIFICATION', 1);

// minimum length of an users password contain (min 5)

define('PASSWORD_LENGTH', 5);

// how many uppercase must the password contain (min 0)

define('PASSWORD_UPPERCASES', 0);

// how many numbers the users must the password contain (min 0)

define('PASSWORD_NUMBERS', 0);

// how many special charachters must the password contain (min 0)

define('PASSWORD_SPECIALCHARS', 0);

// maximal username length

define('USERNAME_MAX_LENGTH', 30);